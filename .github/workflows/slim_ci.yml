name: dbt Slim CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch:  

jobs:
  slim-ci:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dbt-snowflake
        run: |
          pip install dbt-snowflake==1.11.1
      
      - name: Create profiles.yml
        run: |
          mkdir -p ecommerce_pipeline
          cat > ecommerce_pipeline/profiles.yml << EOF
          dbt_snowflake:
            target: ci
            outputs:
              ci:
                type: snowflake
                account: ${{ secrets.SNOWFLAKE_ACCOUNT }}
                user: ${{ secrets.SNOWFLAKE_USER }}
                password: ${{ secrets.SNOWFLAKE_PASSWORD }}
                role: ${{ secrets.SNOWFLAKE_ROLE }}
                database: ${{ secrets.SNOWFLAKE_DATABASE }}
                warehouse: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
                schema: DBT_CI
                threads: 4
          EOF
      
      - name: Get production manifest from main branch
        run: |
          mkdir -p ./state
          echo "📥 Fetching production manifest from main branch..."
          git fetch origin main
          git show origin/main:ecommerce_pipeline/state/manifest.json > ./state/manifest.json 2>/dev/null || echo "⚠️ No manifest found"
          
          if [ -f "./state/manifest.json" ]; then
            echo "✅ Production manifest loaded successfully"
            ls -lh ./state/manifest.json
          else
            echo "⚠️ No production manifest - will run all models"
          fi
      
      - name: Install dbt packages
        run: |
          dbt deps --project-dir ecommerce_pipeline --profiles-dir ecommerce_pipeline
      
      - name: Run dbt on modified models only
        run: |
          if [ -f "./state/manifest.json" ]; then
            echo "🎯 Running Slim CI - only modified models + downstream"
            dbt run --select state:modified+ --defer --state ./state --project-dir ecommerce_pipeline --profiles-dir ecommerce_pipeline || \
            dbt run --project-dir ecommerce_pipeline --profiles-dir ecommerce_pipeline
          else
            echo "⚙️ Running all models - no state available"
            dbt run --project-dir ecommerce_pipeline --profiles-dir ecommerce_pipeline
          fi
      
      - name: Test modified models only
        run: |
          if [ -f "./state/manifest.json" ]; then
            echo "🧪 Testing modified models only"
            dbt test --select state:modified+ --defer --state ./state --project-dir ecommerce_pipeline --profiles-dir ecommerce_pipeline || \
            dbt test --project-dir ecommerce_pipeline --profiles-dir ecommerce_pipeline
          else
            echo "🧪 Testing all models - no state available"
            dbt test --project-dir ecommerce_pipeline --profiles-dir ecommerce_pipeline
          fi
      
      - name: Generate documentation
        if: success()
        run: |
          dbt docs generate --project-dir ecommerce_pipeline --profiles-dir ecommerce_pipeline
      
      - name: Upload run artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dbt-run-artifacts
          path: |
            ecommerce_pipeline/target/run_results.json
            ecommerce_pipeline/target/manifest.json
            ecommerce_pipeline/logs/
